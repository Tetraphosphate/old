// This code is useless, it was used for the 2nd trombone, which was removed.
/* function random() {
    var rand = Math.floor((Math.random() * 2) + 1);
    switch(rand) {
        case 1:
            document.getElementById("SadTrombone").play();
            break;
        case 2:
            document.getElementById("ThePriceIsRightLosingHorn").play();
            break;
            
    }

} */

// This code is useless, it was used for the 3rd trombone, which was removed.
/* function randoms() {

    var rand = Math.floor((Math.random() * 2) + 1);
    switch(rand) {
       case 2:
           document.getElementById("StressedOut").play();
           break;
       case 1: 
         document.getElementById("AddictWithAPen").play();
         break;
    
    }

} */